# Virtual Hearing Engine (Brief Reviewer) configuration

llm:
  provider: "gemini"
  model: "gemini-3-pro-preview"

review:
  # Path to the markdown brief to review
  input_file: "D:\\016_RAG\\project\\code\\output\\brief.md"
  # Where to save the JSON review output
  output_file: "./output/review5.json"
  # Number of personas to sample (int) or "all"
  sample: all
